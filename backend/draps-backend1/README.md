# draps-backend1 — DEPRECATED / INACTIVE

**Status:** Inactive experimental backup. Not part of the formal V2 architecture.

**Context:** This folder was copied over from `entAgentProject21/backend/draps-backend1/` when the codebase was duplicated into `hedgeAdvisor2/` on 2026-05-26. It appears to be an older partial copy of the DRAPS TypeScript backend (only `config/` and `package.json` present; no `src/`).

**The active DRAPS path inside this project is `backend/draps-backend/`** (sibling folder), which has the full `src/`, routes, and MCP server.

**Excluded from:**
- Build pipelines
- Test runs
- Deployment artifacts
- The V2 design (`DESIGN/DESIGN-V1/` and `DESIGN/DESIGN-V1.1/` if/when created)

**Retained because:** the project owner asked to keep it for potential later revival. Do NOT delete without explicit confirmation.

**If revived in the future:** decide whether to merge into `backend/draps-backend/` or move out of `hedgeAdvisor2/backend/` entirely. Either way, this README is removed.
