# Backend dependencies for ACTUS Hedge Advisor.
# Pinned conservatively. Compatibility notes:
#   - fastapi 0.116.1 supports starlette >=0.40.0,<0.48.0 (release notes verified)
#   - mcp 1.x requires starlette >=0.39, which falls inside the fastapi 0.116 window
#   - starlette pin below is the overlap; pip will pick a single version both accept

# Web framework
fastapi==0.116.1
uvicorn[standard]==0.32.0
sse-starlette==2.1.3
starlette>=0.40.0,<0.48.0

# Settings & validation (Pydantic v2)
pydantic==2.9.2
pydantic-settings==2.5.2

# LangGraph + SQLite checkpointer
langgraph==0.2.34
langgraph-checkpoint-sqlite==2.0.0

# Google Gemini SDK
google-generativeai==0.8.3

# MCP client (for DRAPS run_simulation tool)
# Unpinned floor at 1.0.0; let pip pick the latest compatible with starlette range above.
mcp>=1.0.0

# HTTP client (for ACTUS-Mentor REST calls)
httpx==0.27.2

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
